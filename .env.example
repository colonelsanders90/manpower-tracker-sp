# Subsite-relative API base. Always start with the SP site path.
# In dev with the mock-server, this falls through to a Vite proxy.
# In production this is set at build time, OR resolved at runtime via JSOM
# (see App.tsx → getWebServerRelativeUrl).
VITE_SP_API_BASE=/sites/raid/manpower-tracker/_api
